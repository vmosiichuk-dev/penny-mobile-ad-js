export const CLASSES = {
	slider: 'slider',
	slide: 'slide',
	slideItem: 'slide-item',
	slideOut: 'slide-out',
	slideIn: 'slide-in',
};

export const GSAP_CLASSES = {
	slideOut: `gsap-${CLASSES.slideOut}`,
	slideIn: `gsap-${CLASSES.slideIn}`,
};
